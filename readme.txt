README
======

This component is managed by org.gerken.spanish.access.SpanishHttp

Run this class and browse URL http://localhost:8080/spanish

To include this component in a larger application, simply construct an instance SpanishHttp, specifying the
correct port and file system resource root directory.